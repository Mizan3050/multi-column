import { ScaleTemplatePipe } from './scale-template.pipe';

describe('ScaleTemplatePipe', () => {
  it('create an instance', () => {
    const pipe = new ScaleTemplatePipe();
    expect(pipe).toBeTruthy();
  });
});
